for line in $(cat GTExCovKey.csv); do
   full=$(echo $line | awk -F',' '{print $1}')
   abb=$(echo $line | awk -F',' '{print $2}')
	if grep "$abb" tissuesused.txt; then
		sbatch -a 1-100 --export=VCF=$VCF,pheno=$(echo $abb/$abb.pheno.bed.gz),tissue=$(echo $abb/$abb),covariates=$(echo $abb/$full.v7.covariates_output.txt),abb=$abb,full=$full ${scripts}/sh/PermPass.sh
	fi
done

for line in $(cat tissuesused.txt); do
	for i in {1..100}; do
		if [ -f $line/${line}_permutations_chunk_${i}.txt]; then